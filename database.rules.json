{
  "rules": {
    ".read": false,
    ".write": false,
    "rooms": {
      "$room": {
        "config": {
          ".read": "data.exists()",
          ".write": "!data.exists() && newData.hasChildren(['version','verifierCiphertext','verifierIv','verifierSalt','createdAt']) && newData.child('version').val() === 2 && newData.child('verifierCiphertext').isString() && newData.child('verifierCiphertext').val().length > 0 && newData.child('verifierCiphertext').val().length <= 160 && newData.child('verifierIv').isString() && newData.child('verifierIv').val().length > 0 && newData.child('verifierIv').val().length <= 64 && newData.child('verifierSalt').isString() && newData.child('verifierSalt').val().length > 0 && newData.child('verifierSalt').val().length <= 64 && newData.child('createdAt').isNumber()",
          "version": {
            ".validate": "newData.isNumber() && newData.val() === 2"
          },
          "verifierCiphertext": {
            ".validate": "newData.isString() && newData.val().length > 0 && newData.val().length <= 160"
          },
          "verifierIv": {
            ".validate": "newData.isString() && newData.val().length > 0 && newData.val().length <= 64"
          },
          "verifierSalt": {
            ".validate": "newData.isString() && newData.val().length > 0 && newData.val().length <= 64"
          },
          "createdAt": {
            ".validate": "newData.isNumber()"
          },
          "$other": {
            ".validate": false
          }
        },
        "message": {
          ".read": "data.exists() && data.child('expiresAt').isNumber() && data.child('expiresAt').val() > now",
          ".write": "!newData.exists() || (newData.hasChildren(['version','ciphertext','iv','salt','createdAt','expiresAt']) && newData.child('version').val() === 2 && newData.child('ciphertext').isString() && newData.child('ciphertext').val().length > 0 && newData.child('ciphertext').val().length <= 128 && newData.child('iv').isString() && newData.child('iv').val().length > 0 && newData.child('iv').val().length <= 64 && newData.child('salt').isString() && newData.child('salt').val().length > 0 && newData.child('salt').val().length <= 64 && newData.child('createdAt').isNumber() && newData.child('expiresAt').isNumber() && newData.child('expiresAt').val() > now && newData.child('expiresAt').val() <= now + 600000)",
          "version": {
            ".validate": "newData.isNumber() && newData.val() === 2"
          },
          "ciphertext": {
            ".validate": "newData.isString() && newData.val().length > 0 && newData.val().length <= 128"
          },
          "iv": {
            ".validate": "newData.isString() && newData.val().length > 0 && newData.val().length <= 64"
          },
          "salt": {
            ".validate": "newData.isString() && newData.val().length > 0 && newData.val().length <= 64"
          },
          "createdAt": {
            ".validate": "newData.isNumber()"
          },
          "expiresAt": {
            ".validate": "newData.isNumber()"
          },
          "$other": {
            ".validate": false
          }
        }
      }
    }
  }
}
