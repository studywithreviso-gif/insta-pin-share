{
  "rules": {
    ".read": false,
    ".write": false,
    "rooms": {
      "$room": {
        ".read": "data.exists() && data.child('expiresAt').isNumber() && data.child('expiresAt').val() > now",
        ".write": "!newData.exists() || (newData.hasOnly(['version','ciphertext','iv','salt','createdAt','expiresAt']) && newData.child('version').val() === 1 && newData.child('ciphertext').isString() && newData.child('ciphertext').val().length <= 128 && newData.child('iv').isString() && newData.child('iv').val().length <= 64 && newData.child('salt').isString() && newData.child('salt').val().length <= 64 && newData.child('createdAt').isNumber() && newData.child('createdAt').val() <= now + 60000 && newData.child('createdAt').val() >= now - 60000 && newData.child('expiresAt').isNumber() && newData.child('expiresAt').val() > now && newData.child('expiresAt').val() <= now + 600000)"
      }
    }
  }
}
